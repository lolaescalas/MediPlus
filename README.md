Interfaz visual WinForms (.NET 8 / C#) con (Python) para visualizar datos de MongoDB Atlas y Neo4j Aura.

cd backend
python -m venv venv
venv\Scripts\activate
pip install -r requirements.txt
copy .env.example .env
 
 testear conexiones :
 python tests/test_connections.py
